let response = null;

      // if (isExists){
      //     // 3) 파일이 존재할 경우 파일 삭제
      //     fs.unlinkSync(target);
      //     logger.debug(target + "파일삭제완료")
      //     response = "<h1>파일삭제완료</h1>"

      //     res.status(200).send(response);
      // } else {
      //   response = "파일이 없습니다"
      //   res.status(200).send(response)
      // }